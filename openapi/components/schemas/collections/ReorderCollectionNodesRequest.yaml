type: object
required: [ordered_ids]
properties:
  parent_id:
    anyOf:
      - type: integer
        minimum: 1
      - type: 'null'
  ordered_ids:
    type: array
    minItems: 1
    items:
      type: integer
      minimum: 1
additionalProperties: false
