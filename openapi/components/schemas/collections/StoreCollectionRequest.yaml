type: object
required: [type]
properties:
  name:
    type: [string, 'null']
    minLength: 1
    maxLength: 255
  type:
    type: string
    enum: [drive, gallery]
  profile_id:
    type: integer
    minimum: 1
    description: Required when `type` is `gallery`.
  policy:
    type: object
    additionalProperties: true
additionalProperties: false
