parameters:
  - $ref: '../../components/parameters/galleries/UploadId.yaml'
post:
  tags: [Galleries]
  operationId: getGenericUploadPartUrls
  summary: Get signed upload URLs for multiple multipart chunks (generic)
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/uploads/GetUploadPartUrlsRequest.yaml'
  responses:
    '200':
      description: Signed URLs for upload parts
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/galleries/GetGalleryUploadPartUrlsResponse.yaml'
    '401':
      $ref: '../../components/responses/common/Unauthorized.yaml'
    '422':
      $ref: '../../components/responses/common/ValidationError.yaml'
