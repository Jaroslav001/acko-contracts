parameters:
  - $ref: '../../components/parameters/profiles/ProfileSlug.yaml'
get:
  tags: [Profiles]
  operationId: getProfileViewerState
  summary: Get viewer-specific state for profile detail
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Viewer state for current authenticated user
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/profiles/ProfileViewerStateResponse.yaml'
    '401':
      $ref: '../../components/responses/common/Unauthorized.yaml'
    '404':
      $ref: '../../components/responses/common/NotFound.yaml'
